{
  "name": "github-personal-intro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check:blog": "npm run build && node scripts/check-blog.mjs",
    "check:links": "node scripts/check-live-links.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.0",
    "astro": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "rehype-mermaid": "^3.0.0"
  }
}
